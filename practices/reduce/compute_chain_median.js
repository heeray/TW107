'use strict';

function compute_chain_median(collection) {
  //在这里写入代码
  var n=length
}
function lengthV3(head) {
  for (var len = 0, node = head; node; node = node.next)
   len++;
  return len;
}

module.exports = compute_chain_median;
